# components/ui/

Generic UI primitives — buttons, inputs, dialogs, cards, badges. Mostly generated/managed via shadcn/ui. No portfolio-specific content or data-fetching belongs here; these components should be reusable in any project.
