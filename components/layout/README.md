# components/layout/

Shared page chrome used across routes: header, footer, navigation, and page shells. Layout components should not fetch or own content — they compose `components/sections` and `components/admin` around it.
