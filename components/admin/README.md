# components/admin/

Admin panel UI: content editors, tables, and forms for managing projects, jobs, skills, and certifications stored in Supabase. Only rendered behind authentication in `app/admin`.
