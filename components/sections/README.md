# components/sections/

Public-site sections (hero, about, skills, projects, experience, education, contact). Each section renders content passed in as props — it fetches nothing itself. Data comes from `lib/data/*` server-side calls made in `app/`.
