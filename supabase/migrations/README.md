# supabase/migrations/

One SQL file per schema change, named `<timestamp>_<description>.sql` (the format the Supabase CLI generates via `supabase migration new <description>`). Migrations are the only sanctioned way to change the database schema.
