# types/

Shared TypeScript types and Zod schemas for every content model (Project, Job, Skill, Certification, etc.). These types should mirror the Supabase schema defined in `supabase/migrations` and are the single source of truth consumed by both `lib/data` and the components that render that data.
